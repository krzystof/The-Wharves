nginx:
    image: krzystof/the-wharves-nginx:latest
    ports:
        - "80:80"
    links:
        - fpm
    volumes:
        - .:/code/
fpm:
    image: krzystof/the-wharves-php7fpm:latest
    environment:
        APP_ENV: local
    expose:
        - "9000"
    volumes:
        - .:/code/
