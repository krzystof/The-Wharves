parameters:
    bin_dir: "./vendor/bin"
    git_dir: "."
    ascii:
        failed: grumphp-grumpy.txt
        succeeded: grumphp-happy.txt
    tasks:
        phpcs:
            standard: PSR2
            ignore_patterns:
                - Test
        phpunit:
            config_file: ~
        yamllint: ~
    extensions: []
